package main

// func swap(pa, pb *float64) {
// 	// n := *pa
// 	// *pa = *pb
// 	// *pb = n
// 	*pa, *pb = *pb, *pa
// }

func main() {
	// p := new(int)
	// fmt.Printf("var p is of type %T and of value %v\n", p, p)
	// x := 100
	// p = &x
	// fmt.Printf("var p is of type %T and of value %v\n", p, p)

	// var p *int
	// fmt.Printf("var p is of value %v\n", p)

	// x := 10.10
	// fmt.Printf("the address of x is: %p\n", &x)
	// ptr := &x
	// fmt.Printf("var ptr is of type %T and is of value %v\n", ptr, ptr)
	// fmt.Printf("the address of the pointer is: %p\n", &ptr)
	// fmt.Printf("the value of x is: %f\n", *ptr)

	// x, y := 10, 2
	// ptrx, ptry := &x, &y
	// z := *ptrx / *ptry
	// fmt.Println(z)
	// fmt.Printf("var z is of type %T\n", z)

	// x, y := 5.5, 8.8
	// swap(&x, &y)
	// fmt.Println(x, y)
}
